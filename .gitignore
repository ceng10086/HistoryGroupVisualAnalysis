# ── CBDB 數據（體積巨大且來自上游公開倉庫，不入庫；按 README 的步驟自行下載） ──
cbdb_sqlite/

# ── Node.js ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnp.*

# ── 操作系統 / 編輯器 ──
.DS_Store
Thumbs.db
*.swp
*.swo
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json

# ── Claude / IDE 私有目錄 ──
.claude/
.playwright-mcp/

# ── 構建 / 緩存 ──
dist/
build/
coverage/
.cache/
*.log

# ── 環境變量 / 私密 ──
.env
.env.*
*.pem
*.key

# ── 平台特定 ──
*.tmp
*.bak
